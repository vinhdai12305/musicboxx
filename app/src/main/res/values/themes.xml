<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- BASE THEME -->
    <style name="Base.Theme.Musicboxx"
        parent="Theme.Material3.DayNight.NoActionBar" />

    <!-- APP THEME -->
    <style name="Theme.MusicBox"
        parent="Theme.Material3.DayNight.NoActionBar">

        <!-- PRIMARY -->
        <item name="colorPrimary">#FF8D28</item>
        <item name="colorOnPrimary">@android:color/white</item>

        <item name="colorSecondary">#FF8D28</item>
        <item name="colorOnSecondary">@android:color/black</item>

        <!-- MATERIAL 3 SURFACE (QUAN TRỌNG NHẤT) -->
        <item name="colorSurface">@android:color/white</item>
        <item name="colorSurfaceContainer">@android:color/white</item>
        <item name="colorSurfaceVariant">@android:color/white</item>

        <!-- STATUS BAR -->
        <item name="android:statusBarColor">@android:color/white</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">
            true
        </item>

        <!-- APP BAR -->
        <item name="appBarLayoutStyle">@style/AppBarWhite</item>

        <!-- BOTTOM SHEET -->
        <item name="bottomSheetDialogTheme">
            @style/AppBottomSheetDialogTheme
        </item>
    </style>

    <!-- APP BAR STYLE -->
    <style name="AppBarWhite"
        parent="Widget.Material3.AppBarLayout">
        <item name="android:background">@android:color/white</item>
        <item name="elevation">0dp</item>
    </style>

    <!-- BOTTOM SHEET -->
    <style name="AppBottomSheetDialogTheme"
        parent="Theme.Material3.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/AppModalStyle</item>
    </style>

    <style name="AppModalStyle"
        parent="Widget.Material3.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

</resources>
