<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/bottom_sheet_background"
    android:orientation="vertical"
    android:paddingBottom="24dp">


    <!-- Thanh kéo nhỏ ở trên cùng -->
    <View
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="16dp"
        android:background="@drawable/bottom_sheet_handle" />

    <!-- Thông tin bài hát -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:paddingBottom="16dp">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/song_album_art"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:scaleType="centerCrop"
            app:shapeAppearanceOverlay="@style/ShapeAppearance.Material3.Corner.Medium"
            tools:src="@drawable/con_gi_dep_hon" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_marginStart="16dp"
            android:layout_toStartOf="@id/favorite_button"
            android:layout_toEndOf="@id/song_album_art"
            android:orientation="vertical">

            <TextView
                android:id="@+id/song_title_bottom_sheet"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="#1C1C1C"
                android:textSize="16sp"
                android:textStyle="bold"
                tools:text="Phép Màu" />

            <TextView
                android:id="@+id/song_artist_bottom_sheet"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="#606060"
                android:textSize="12sp"
                tools:text="feat. Minh Tóc... | 03:50 mins" />
        </LinearLayout>

        <ImageView
            android:id="@+id/favorite_button"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_alignParentEnd="true"
            android:layout_centerVertical="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:padding="12dp"
            android:src="@drawable/ic_favorite_gray" />
    </RelativeLayout>

    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#1F000000" />

    <!-- Danh sách các tùy chọn -->
    <LinearLayout
        android:id="@+id/options_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingTop="8dp">

        <!-- Bạn có thể tạo 1 file layout item_option.xml và dùng RecyclerView ở đây -->
        <!-- Nhưng để đơn giản, chúng ta sẽ dùng các TextView riêng lẻ -->
        <TextView style="@style/BottomSheetOption" android:id="@+id/option_play_next" android:drawableStart="@drawable/ic_play_next" android:text="Play Next"/>
        <TextView style="@style/BottomSheetOption" android:id="@+id/option_add_to_queue" android:drawableStart="@drawable/ic_add_play_queue" android:text="Add to Playing Queue"/>
        <TextView style="@style/BottomSheetOption" android:id="@+id/option_add_to_playlist" android:drawableStart="@drawable/ic_playlist_gray" android:text="Add to Playlist"/>
        <TextView style="@style/BottomSheetOption" android:id="@+id/option_go_to_album" android:drawableStart="@drawable/ic_album" android:text="Go to Album"/>
        <TextView style="@style/BottomSheetOption" android:id="@+id/option_go_to_artist" android:drawableStart="@drawable/ic_artist" android:text="Go to Artist"/>
        <TextView style="@style/BottomSheetOption" android:id="@+id/option_details" android:drawableStart="@drawable/ic_detail" android:text="Details"/>
        <TextView style="@style/BottomSheetOption" android:id="@+id/option_set_as_ringtone" android:drawableStart="@drawable/ic_phone" android:text="Set as Ringtone"/>
        <TextView style="@style/BottomSheetOption" android:id="@+id/option_add_to_blacklist" android:drawableStart="@drawable/ic_add_blacklist" android:text="Add to Blacklist"/>
        <TextView style="@style/BottomSheetOption" android:id="@+id/option_share" android:drawableStart="@drawable/ic_share" android:text="Share"/>
        <TextView style="@style/BottomSheetOption" android:id="@+id/option_delete" android:drawableStart="@drawable/ic_delete" android:text="Delete form Device" android:textColor="#E53935"/>

    </LinearLayout>
</LinearLayout>
